# Pattern number six

> definition: Depend upon abstractions. Do not depend upon concrete classes.

This is called dependency inversion. In the pizza shop by using factory pattern we do this. We don't create pizzas in the pizza stores. We have a factory class that will manage create classes. The pizza store itself is depend on only the factory and the product itself that is pizza.
