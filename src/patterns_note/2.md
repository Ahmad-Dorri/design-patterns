# Pattern number two

Program to an interface, not an implementation.

> details in the duck application:

In duck application, we created two interfaces and create different type of behaviors using these interfaces. With this we now exactly what is the shape of each behavior is. We can tell each class to have a fly or quack behavior but we don't force it to have a single behavior and we can change the behavior in runtime if we code to an interface.
