# Pattern number five

Classes should open for extension, but closed for modification.

> Note: this is the important rule in SOLID. Open-Closed principle. When creating classes we should think about the future. If in the future we want to add another feature and we changed previous classes this is not Open-Closed. We should extend previous classes to introduce new code and implement that feature.
