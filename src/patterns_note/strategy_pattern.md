# Definition

Strategy pattern defines a family of algorithms, encapsulates each one, and make them interchangeable. Strategy let's the algorithm vary independently from clients that use it.

> Note: In duck application we use this pattern to move out changeable parts like quacking and flying behaviors from static one like display. By using this we can change these behaviors in runtime.

